import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ElementosproteccionPage } from './elementosproteccion.page';

describe('ElementosproteccionPage', () => {
  let component: ElementosproteccionPage;
  let fixture: ComponentFixture<ElementosproteccionPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ElementosproteccionPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
